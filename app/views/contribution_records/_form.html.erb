<%= form_for(@contribution_record) do |f| %>
  <% if @contribution_record.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@contribution_record.errors.count, "error") %> prohibited this contribution_record from being saved:</h2>

      <ul>
      <% @contribution_record.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
